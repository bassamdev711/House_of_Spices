/**
 * Compatibility entry point for the disposable demo catalog.
 *
 * The catalog is maintained in prisma/seed.ts so every seed command uses
 * the same Arabic spice, tea, coffee, herbs, saffron, and blend data.
 */
import '../prisma/seed';
